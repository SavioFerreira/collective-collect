export type ColetaDTO = {
  
		id: number,
		status: string,
		type: string,
		gravity: string,
		date: string,
		image: string,
		title: string,
		description:string,
		locale: string,
		complaintId: number,
		collaborators: [],
	}